<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.yedam.persistence.BookMapper">

	<insert id="insert" parameterType="com.yedam.domain.BookVO">
		insert into book_tbl_06 
		values ((
        		select max(#{bookNo}) from book_tbl_06 ) + 1,
        		#{bookName} , #{bookCoverimg} , #{bookDate} , #{bookPrice} , #{bookPublisher} , #{bookInfo} 
        		)
	</insert>
	

</mapper>